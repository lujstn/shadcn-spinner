{
  "name": "shadcn-spinner-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "node ../scripts/build-registry.js && next build",
    "start": "next start",
    "lint": "next lint"
  }
}